# server/.env.test

# This MUST be the same secret as in your main .env file
JWT_SECRET="YOUR_REALLY_SECRET_AND_LONG_KEY_HERE"

# This should point to a SEPARATE database from your development one.
# It can be on the same PostgreSQL server, but it must have a different name.
# Example:
DATABASE_URL="postgresql://postgres:Divinity:100@localhost:5432/mentorship_platform_test?schema=public"